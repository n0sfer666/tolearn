# Контекст проекта tolearn

Локальное десктоп-приложение для обучения по программам (бандлам), которые
генерирует LLM. Полностью офлайн, без аккаунтов, ключей и платных функций.
Экзаменует LLM, но через копипаст: промпт копируется в любой чат, вердикт
вставляется обратно.

Что это и зачем — [README.md](../README.md) в корне. Здесь только то, что нужно
агенту для работы и чего нет в корневых документах.

## Источники правды (читать до кода)

| Вопрос | Где ответ |
|---|---|
| ограничения, бюджеты, структура крейтов | [docs/architecture.md](../docs/architecture.md) |
| принятые решения и отвергнутые альтернативы | [docs/adr/](../docs/adr/) — 10 файлов |
| что делаем и в каком порядке | [docs/specs/README.md](../docs/specs/README.md) — 58 спек |
| вердикт → статус, форма попытки | [docs/protocol.md](../docs/protocol.md) |
| конспекты, шифрование, осиротевшие | [docs/notes.md](../docs/notes.md) |
| офлайн: стратегии по типам материалов, кэш | [docs/offline.md](../docs/offline.md) |
| цвет, глифы, токены, модель видимости | [docs/design/visual-system.md](../docs/design/visual-system.md) |
| порядок экранов и секций | [docs/design/navigation.md](../docs/design/navigation.md) |
| дизайн-токены (единственный источник значений) | [docs/design/tokens.css](../docs/design/tokens.css) |

Эталонный бандл `examples/llm-agents-base/` — одновременно пример формата и
главная тестовая фикстура. Менять его нельзя: тесты пиннят его содержимое.

## Как ведётся работа

Строго по спекам, по критическому пути, по порядку. TDD: тест пишется до кода.
Спека закрыта только когда её DoD проверен **запуском**, а не рассуждением.
Текущее состояние — [progress.md](progress.md).

## Правила, которые не пересматриваются без слова пользователя

1. Rust-ядро + Tauri 2; UI — статическая Astro MPA + острова SolidJS. Никакого
   SPA-роутера, никакого SSR, никаких веб-шрифтов.
2. `core` не знает про Tauri и UI; `offline` не знает про Tauri. Направление
   зависимостей — только вниз.
3. Никаких SDK, аккаунтов, ключей и платных функций. Приложение работает без
   сети; тесты — тем более (фикстуры вместо запросов).
4. Бюджеты из [architecture.md](../docs/architecture.md#бюджеты) — это тесты,
   а не пожелания. Числа ужесточаются по замерам, не ослабляются.
