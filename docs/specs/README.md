# Спецификации

Работа разбита на маленькие спеки: у каждой один результат, наблюдаемый критерий
готовности и тесты, которые пишутся **до** кода.

## Правила

- Одна спека — одна ответственность. Не помещается в описание из трёх строк —
  делится дальше.
- Спека закрыта, когда её DoD проверен запуском, а не рассуждением.
- Спека не начинается, пока не закрыты её зависимости. Критический путь ниже
  обязан быть согласован с колонкой «зависит».
- Тесты работают на фикстурах и **без сети**. Эталонная программа в `examples/` —
  основная фикстура; всё, чего в ней нет (глубина, битые документы и деревья),
  добавляется в корпус `fixtures/v2/` явно, а не выдумывается по месту в
  каждом тесте.

## Статусы

`todo` · `in progress` · `done`

## Слой 0 — фундамент

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S01 | Каркас воркспейса, toolchain, CI | — | `cargo test` и `cargo clippy -- -D warnings` зелёные в CI; все крейты из [структуры](../architecture.md#структура) заведены пустыми | T2 |
| S02 | Контракт схем и корпус фикстур | S01 | три JSON Schema (`roadmap`, `topic`, `progress`) описывают **все** поля эталона; эталон валиден; корпус валидных фикстур покрывает историю попыток, циклы зависимостей, `paywall`/`login_required`, `video`, расхождение `defaults` и поля темы; ≥12 битых фикстур отвергаются с **разными** кодами ошибок | T2 |

Перечень полей для S02 задаётся исчерпывающе, иначе DoD «разбирается полностью»
у S03/S04 непроверяем. По каждому полю фиксируется одно из трёх: рендерится /
влияет на логику / игнорируется. Отдельного внимания требуют `priority`
(влияет на знаменатель прогресса), `stale` и `delta` у материала (показываются),
`smoke_checked` (показывается перед запуском команды), `calibration.passed_out`
(источник статуса `passed_out`).

## Слой 1 — ядро

Чистые функции над данными: вход — байты, выход — тип или ошибка.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S03 | Парсер `roadmap.yaml` | S02 | эталон разбирается со всеми полями, включая `calibration`, `version_pins`, `env_constraints`; неизвестное поле не роняет разбор; ошибка несёт строку и колонку | T1 |
| S04 | Парсер `topics/*.yaml` | S02 | `local-runtime.yaml` (443 строки) разбирается без потерь; `constraints[]` и `acceptance[]` — две **разные** коллекции; блочные скаляры сохраняют переводы строк | T1 |
| S05 | Валидатор целостности | S03,S04 | ловит: дубли `id`, `depends_on` в никуда, цикл в графе, чекпойнт без покрытия этапа, `stage` вне диапазона, несовпадение мажорных версий схем внутри бандла, потерянный файл темы в этапе с `generated: true` | T1 |
| S05a | Строгость документа | S03,S04,S05 | документ, который отвергает схема S02, отвергается и на входе: дубль ключа в маппинге — ошибка разбора со строками **обоих** вхождений, а не «побеждает последний»; пустая строка там, где схема требует `minLength: 1`; `stage`, `retry_after_days`, `weekly_hours` вне диапазона схемы; `passed_at`, `next_review_at` и `at` — RFC 3339 либо `null`. `est_hours` с `min > max` ловит валидатор S05 отдельным нарушением `bundle.*`, а не парсер. Каждый случай — отдельный код ошибки и битая фикстура в корпусе | T1 |
| S06 | `progress.yaml`: чтение и запись | S02 | форма `attempts[]` по [протоколу](../ru/protocol.md#форма-попытки) с сохранением `raw`; запись атомарна (tmp+rename); неизвестные ключи чужих версий сохраняются; порядок тем стабилен | T2 |
| S07 | Статусы и производные | S05,S06 | `blocked` из графа; `stale_passed` из `verified_at + revalidate_after_days`; поле темы перекрывает `defaults` (фикстура с несовпадающими значениями); `next_review_at` из `retention`; `passed_out` из `calibration.passed_out`; таблица переходов покрыта целиком | T1 |
| S08 | Ручное управление статусом | S07 | любой статус ставится без зачёта; отметка пишется с `source: manual`; ручной сброс не стирает `attempts[]`; статистика отличает ручное от зачтённого | T1 |
| S09 | Сканер бандла | S03,S04 | находит корень по `roadmap.json` или `roadmap.yaml`, формат бандла определяется тем, какой из них лежит; лежат оба — приоритет у `roadmap.yaml`; тема без файла в неcгенерированном этапе → «не сгенерирована»; битый файл темы деградирует до заглушки, остальные читаются | T1 |
| S10 | Слияние при перегенерации | S09,S06 | прогресс и конспекты сохраняются по `id`; изменившееся наполнение понижает `passed` до `stale_passed`; исчезнувшая тема даёт осиротевшую запись, а не удаление; отчёт о слиянии перечисляет каждый случай | T2 |
| S11 | Агрегация прогресса | S07 | сводка по этапу и программе: пройдено/всего, часы, доля; несгенерированные темы не в знаменателе; `priority` учитывается по правилу из S02 | T1 |
| S12 | Рендер промпта экзаменатора | S03,S04 | промптом является **только** фрагмент между первым и вторым `---` (в эталоне это строки 7 и 138); служебный заголовок и хвост «Что трекер делает» вырезаны; все `{{...}}` подставлены, включая `roadmap.*`; пустые секции вырезаны, а не оставлены заголовками; совпадение с golden-файлом | T1 |
| S13 | Парсер вердикта | S02 | берёт **последний** JSON-блок из свободного текста; отвергает вердикт без `per_question` и с чужим `topic_id`; частичный разбор с перечнем недостающих полей | T1 |
| S14 | Применение протокола | S13,S07,S06 | таблица «вердикт → статус» из [protocol.md](../ru/protocol.md) покрыта целиком; `blocked` не пишется в статистику и не считается в три провала подряд; три `fail` подряд дают предложение разделить тему | T1 |
| S15 | Реестр программ | S06 | список бандлов переживает перезапуск; недоступный путь помечается, а не удаляется молча | T2 |

Граница между S05a и соседями. Схема S02 — контракт, но она работает только в
тестах: приложение JSON Schema не гоняет. S05a закрывает разрыв — всё, что схема
объявила про **отдельное значение** (тип, длина, диапазон, формат даты), парсер
проверяет сам, и корпус битых фикстур доказывает это по одному случаю на код
ошибки. Всё, что про **связь двух значений** (`min > max`, дубли `id`, ссылки в
никуда), остаётся у валидатора S05, потому что оттуда виден весь бандл разом и
нарушения собираются списком, а не роняют документ на первом.

Дубль ключа — единственное требование S05a, которого не видно из готового дерева:
`saphyr` кладёт маппинг в `LinkedHashMap`, и второе вхождение молча затирает
первое. Ловится на уровне событий (`saphyr_parser::SpannedEventReceiver`), откуда
и берутся строки обоих вхождений.

## Слой 2 — исполнение и CLI

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S16 | Раннер check-команд | S04 | таймаут прерывает команду и убивает **дерево** процессов; лимит объёма вывода; рабочая директория задаётся явно; интерактивная команда не вешает раннер (stdin закрыт); запуск невозможен без явного вызова | T2 |
| S17 | CLI: `validate`, `scan`, `progress`, `exam`, `merge` | S03–S16 | на эталоне все команды успешны; на битых фикстурах код возврата ≠ 0 и внятное сообщение; `--json` даёт машинный вывод; ни одна команда не ходит в сеть | T2 |

## Слой 3 — офлайн

Сеть в тестах не используется: сохранённые ответы лежат фикстурами. Прогон по
живым сайтам — отдельный необязательный сценарий, не входящий в CI.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S18 | Хранилище с адресацией по содержимому | S01 | один URL из двух программ хранится один раз; вытеснение по LRU при превышении бюджета; объекты программ с включённым офлайном защищены; индекс переживает аварийное завершение | T2 |
| S19 | Загрузка статической страницы | S18 | результат не ссылается наружу: ни одного внешнего `src`/`href`; объявлен трейт `Prerenderer` с заглушкой «вернуть исходный HTML», реализация на WebView — в S37 | T2 |
| S20 | Читалка | S19 | извлекает основной текст из 5 сохранённых фикстур разной вёрстки; при неудаче отдаёт исходный архив, а не пустоту | T2 |
| S21 | Зеркало сайта документации | S19 | `llms.txt` обнаруживается и предпочитается; `robots.txt` соблюдается; краулер держит домен, глубину и потолок числа страниц; ссылки между сохранёнными страницами переписаны на локальные | T2 |
| S22 | Материалы типа `repo` | S18 | `gix clone --depth 1`; объём принятых данных ограничен, превышение прерывает клон | T2 |
| S23 | Внешние бинарники и видео | S18 | нет `yt-dlp` или `ffmpeg` → действие неактивно с командой установки для текущей ОС, а не с ошибкой; есть → скачивание с прогрессом и отменой, по умолчанию не выше 720p | T2 |
| S24 | Очередь и переключатель офлайна | S19–S23 | свитч выгружает все материалы программы; материалы с `liveness` `paywall` или `login_required` пропускаются с указанием причины; отчёт «скачано / пропущено / провалено» с разбивкой; отмена в любой момент; повтор только провалившихся | T3 |

## Слой 4 — оболочка и экраны

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S25 | Tauri-оболочка и контракт IPC | S17 | все команды объявлены в одном месте, типы для UI генерируются оттуда же, рукописная команда мимо описания ломает тест; ошибка ядра доезжает до UI как код + сообщение, не как строка паники | T2 |
| S26 | Сборка Astro MPA | S25 | статические HTML по маршрутам; в бандле нет роутера; вес JS уложен в [бюджеты](../architecture.md#бюджеты) и проверяется тестом | T2 |
| S27 | Токены и примитивы | S26 | `docs/design/tokens.css` подключается напрямую, копии нет; линт запрещает hex, px и числа брейкпойнтов вне токенов и падает на нарушении; линт проверяет [конвенцию именования](../design/visual-system.md#токены) — префикс семейства для шкал, одиночное имя вне их, семейство из одного члена отвергается; контраст 4.5:1 для текста и 3:1 для `--color-border-strong` проверяется на обеих темах и всех трёх поверхностях | T2 |
| S28 | Локализация | S27 | смена языка без перезапуска; плюрализация по CLDR; отсутствующий ключ роняет тест, а не молча падает в другой язык | T2 |
| S29 | Темы оформления | S27 | системная/светлая/тёмная; выбор переживает перезапуск; нет вспышки не той темы при старте | T3 |
| S30 | Список программ и импорт | S15,S10,S28,S29 | drag-n-drop и выбор папки дают один результат; импорт битой папки объясняет причину; повторный импорт показывает отчёт о слиянии; прогресс виден по каждой программе | T3 |
| S31 | Экран программы | S11,S30 | лента этапов; статус читается без цвета; чекпойнт визуально отличается от темы; заблокированная тема сообщает, чем разблокируется | T3 |
| S32 | Экран темы | S04,S08,S31 | порядок секций по navigation.md (заменён [ia.md](../design/ia.md) в S109); [модель видимости](../design/visual-system.md#модель-видимости) соблюдена, скрытых полей нет и в исходнике страницы; вырожденный чекпойнт не рисует шесть пустых секций; статус ставится вручную отсюда | T3 |
| S33 | Экран практики | S16,S32 | `constraints` и `acceptance` показаны как две коллекции; текст команды и `smoke_checked` видны **до** запуска; запуск только по явному действию; вывод рядом с ожиданием; отметку ставит человек | T3 |
| S34 | Конспекты | S32 | по notes.md (файл снят в S85): фронтматтер держит привязку, путь — нет; внешний каталог; правка снаружи подхватывается; конкурентная правка показывает обе версии и не сливает молча; осиротевшие не удаляются | T3 |
| S35 | Зачёт: копипаст-цикл | S12,S14,S32 | промпт копируется одним действием; вставляется ответ целиком, не выдранный JSON; статус обновляется по протоколу; `blocked` не попадает в статистику | T3 |
| S36 | Экран разбора | S14,S35 | пробелы из вердикта связаны с вопросами; три `fail` подряд предлагают разделить тему и дают готовый текст запроса | T3 |
| S37 | Пререндер JS-страниц | S19,S25 | реализация `Prerenderer` на скрытом окне WebView; страница с клиентским рендерингом сохраняется с содержимым; таймаут закрывает окно и не вешает очередь | T3 |
| S38 | Настройки | S24,S28,S29,S34 | бюджет диска, каталог конспектов, язык, тема; изменения применяются без перезапуска | T2 |
| S39 | Поиск | S09,S34,S24 | ищет по темам, материалам и конспектам; индекс инкрементальный; работает офлайн | T2 |
| S40 | Клавиатура и доступность | S30–S36 | полное управление с клавиатуры; видимый фокус на каждом интерактивном элементе; `Esc` — на уровень вверх; `/` — поиск; порядок фокуса совпадает с порядком чтения | T3 |

## Слой 5 — опциональный LLM

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S41 | Настройка провайдера | S38 | ключ в системном хранилище, не в конфиге; OpenAI-совместимый endpoint + Ollama; проверка соединения; выключено по умолчанию | T2 |
| S42 | Встроенный экзаменатор | S41,S35 | тот же протокол и тот же вердикт; отключение возвращает копипаст-цикл без потери данных | T3 |
| S62 | Три вида провайдера и CLI-харнесс | S41,S42,S16,S38 | radio из трёх видов (локальная модель, HTTP API по ключу, CLI-харнесс) с отдельной панелью и **двумя** ступенями проверки у каждого: дешёвая («список моделей» либо «бинарник ответил на `--version`») и микро-запрос, доказывающий авторизацию. Харнесс задаётся реестром пресетов (Claude Code, opencode, pi, «Своя команда»), `command` и `args` правятся руками. Запуск изолирован: пустой временный cwd, промпт в stdin, настраиваемый таймаут, убийство **дерева** процессов, потолок вывода; код возврата ≠ 0, обрезка и пустой stdout — три разных отказа, а не молчаливый частичный ответ. Конфиг переезжает на `tolearn/provider/v2` с тремя секциями и полем `active`; файл v1 читается и переносится, ключ в keychain не теряется; переключение вида не стирает чужие поля. `runner` получает кроссплатформенный `spawn` (программа с аргументами, stdin, без шелла), `provider` начинает от него зависеть. Кнопка на `/exam/` называет активный вид. Под каждым вариантом radio сказано, покидают ли данные машину | T2 + T3 |
| S63 | Рекомендации локальных моделей и параметры | S62 | вшитая таблица моделей с весом сверяется с объёмом RAM машины (`sysinfo`): видно, что пойдёт, что тяжеловато и что уже установлено; для недостающих показана готовая команда `ollama pull`. Выбранная модель отсутствует у Ollama → проверка **провалена** с этой командой, а не зелёная с сюрпризом на зачёте. Поля `num_ctx` и `temperature` задаются и уходят в запрос: при дефолтных 2048 токенах длинный промпт экзаменатора молча обрезается | T2 + T3 |
| S64 | Журнал запросов к модели | S62,S38 | галочка в настройках, **выключена** по умолчанию: включённая пишет промпт и ответ целиком в `<app-data>/llm-log/`, держит последние N записей и удаляет старые; рядом «Открыть папку» и «Очистить». Выключенная не оставляет на диске ни байта — проверяется, а не обещается | T2 |
| S66 | Диалоговый зачёт и видимые входы | S42,S62,S44 | вход в проверку — главное действие на экране темы и ход из очереди повторений, а не ссылка внутри `/exam/`. Модель ведёт разговор: вопросы темы идут **по одному**, добивающие уточнения разрешены, отсебятина вместо вопросов темы — нет, потому что вердикт обязан лечь на `per_question`. Кнопка подсказки помечает вопрос `hinted: true` и видна в статистике (S52). Завершение предлагает модель, решает человек; «Завершить» доступно на любом вопросе; вердикт того же формата применяет тот же протокол. Незаконченный диалог живёт в app-data — один на тему, переживает перезапуск, исчезает по вердикту либо по «Начать заново»; попытка в `attempts[]` появляется **только** по вердикту. В контекст уходит тема и текущий вопрос, а не вся переписка: иначе мелкая модель упирается в `num_ctx`. Время и токены каждой реплики видны | T3 |
| S67 | Зачёт по программе | S66,S31 | проверка идёт поперёк тем одной программы, а не по одной теме: приложение само выбирает темы, диалог смешивает их, вердикт выносится по **каждой** затронутой теме отдельно — общего «вердикта по программе» в протоколе нет и не заводится | T3 |

Почему у S62 три вида, а не два. «Локально» и «через чужой CLI» — разные
способы получить ответ, но OpenAI-совместимый endpoint из S41 не сводится ни к
одному из них: у него ключ в системном хранилище и своя ветка запроса. Свести
его внутрь «локального» значило бы спрятать поле ключа под чужим заголовком, а
выбросить — потерять работающий код ради симметрии интерфейса.

Почему проверка двухступенчатая. У HTTP-видов «сервер ответил списком моделей»
почти всё и доказывает. У харнесса «бинарник нашёлся» не доказывает ничего:
`claude --version` отвечает и без единого логина, а отказ вылезет на зачёте,
когда человек уже написал ответ. Поэтому вторая кнопка шлёт настоящий
микро-запрос — и она одинаковая у всех трёх видов, потому что у локальной
модели ровно так же видно, что она грузится минуту и не влезает в память.

Решения по S62–S64 целиком, включая форму конфига, коды отказов и разбор
краевых случаев, — в [.context/specs/2026-08-04-provider-flavors.md](../../.context/specs/2026-08-04-provider-flavors.md).

Почему S66 и S67 разделены. Разговор по одной теме и сквозной прогон поперёк
программы отличаются не объёмом, а тем, кто выбирает вопрос: в первом случае
список фиксирован бандлом, во втором его строит приложение по статусам и датам
повторения. Смешать их значило бы решать обе задачи одним экраном до того, как
первая проверена на живой модели. Решения по S65–S67 —
в [.context/specs/2026-08-06-generation-and-dialog-exam.md](../../.context/specs/2026-08-06-generation-and-dialog-exam.md).

## Слой 6 — генерация программ

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S43 | «Абсолютный» промпт генерации | S02,S17 | промпт поставляется в приложении и содержит машинную процедуру самопроверки; эталонные бандлы, сгенерированные по нему, лежат фикстурами и проходят `tolearn validate` в CI без сети. Проверка «модель попадает в формат с первого раза» — ручной сценарий вне CI: она недетерминирована и требует ключа | T2 |
| S65 | Генерация программы активным провайдером | S43,S62 | кнопка «+» на `/programs/` — главное действие рядом с импортом; провайдер выключен → она неактивна и называет, чем включается, а не исчезает. Спрашивается одно свободное поле «чему хочу научиться» плюс уровень, часы в неделю и срок; язык бандла — язык интерфейса. Промпт S43 делится надвое, и гейт полноты `cli/tests/gate/prompt.rs` делится вместе с ним: `generate-roadmap.md` даёт скелет, `generate-topic.md` — тему за запрос, объединение двух требований покрывает прежние пути схем целиком, и это проверено тестом. После скелета видна оценка объёма и времени, дальше — только по подтверждению; отмена доступна на любом шаге. Ответ не прошёл разбор или валидацию → нарушения `bundle.*` уходят модели обратно, до трёх кругов на артефакт, круги видны. Собранный бандл валидируется **в памяти**, сводка (этапы, темы, часы) показывается до записи, на диск он ложится по «Принять» — в `<app-data>/bundles/<id>`; отказ, исчерпанные круги и отмена не оставляют полубандла. У каждого шага видно время, токены — когда провайдер их сообщил. Перегенерации существующей программы здесь нет: генерация создаёт новый каталог и в чужой бандл не пишет (ADR-009). Транспорт готов заранее: потолок вывода и таймаут S62 вмещают сотни килобайт и минуты работы, и это закрыто тестом там, а не здесь. Оба промпта прогоняются вручную на живой модели через CLI **до** экрана | T2 + T3 |

## Слой 7 — расширения

Критический путь не блокируют. Часть из них требует данных, которых сегодня нет
ни в эталоне, ни в формате, — это сказано прямо в их DoD, а не спрятано: S44
нужна фикстура с непустыми `next_review_at` (в эталоне они все `null`), S50
расширяет схему `progress/v1` и правит [protocol.md](../ru/protocol.md), S49
заводит собственное хранилище версий в app-data. Поэтому все три висят на S02
(корпус фикстур), а S50, который меняет формат записи, — ещё и на S06.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S44 | Очередь повторений | S02,S07,S11,S31 | сквозная очередь «повторить сегодня» поперёк всех программ по `next_review_at`; просроченное отделено от сегодняшнего; повторение меняет дату, а не статус. Требует фикстуры `progress.yaml` с непустыми `next_review_at`: в эталоне они все `null` | T2 |
| S45 | Планирование и прогноз | S11,S31 | дневная норма из `weekly_hours`; прогноз даты завершения — **интервал** из остатка `est_hours`, потому что `est_hours` сам интервал (`[4, 6]`); формула показана; несгенерированные темы помечены как неизвестный остаток и в интервал не входят | T2 |
| S46 | Граф зависимостей | S05,S07,S31 | DAG рисуется из `depends_on`; видно, что тема разблокирует; `blocked` отличим от пройденного без цвета; узел кликабелен | T3 |
| S47 | Экспорт программы в Markdown | S09,S34 | один файл с оглавлением по этапам, темами и конспектами; вывод валиден по CommonMark и проходит markdownlint; внутренние ссылки резолвятся. Поля из [модели видимости](../design/visual-system.md#модель-видимости) экспортируются **только** для тем в статусе `passed` или `stale_passed` — экспорт строже экрана намеренно: файл уходит из приложения, переживает сброс статуса и попадает в чужие руки, а экран показывает то же самое одному человеку и ровно сейчас; общей галочки «включить ответы» нет | T2 |
| S48 | Импорт из архива | S02,S30 | `.zip` и `.tar.gz` дают тот же результат, что папка; вложенный корень бандла находится. Отвергаются: `..` в пути, абсолютный путь, симлинк или хардлинк за пределы корня, превышение потолка распакованного объёма, числа записей и коэффициента сжатия. Распаковка во временный каталог, пути канонизируются **после** распаковки | T2 |
| S49 | История версий бандла | S02,S10,S30 | предыдущая версия сохраняется при перегенерации в app-data, вне бандла и вне LRU-кэша (ADR-005); дифф показывает добавленное, переписанное и причину каждого понижения `passed` до `stale_passed`; глубина истории и её доля в бюджете диска настраиваются. **Только чтение и дифф** — приложение в бандл не пишет (ADR-009), отката нет | T2 |
| S50 | Таймер практики | S02,S06,S33 | обратный отсчёт по `time_box_min`; пауза, сброс, переживает переход между экранами; истечение не блокирует работу. Факт «практика заняла X, таймбокс истёк» пишется в отдельную запись практики, а не в `attempts[]`: попытка появляется только по вердикту зачёта, а таймер истекает раньше. Требует расширения схемы `progress/v1` и правки [protocol.md](../ru/protocol.md) | T2 |
| S51 | Дайджест устаревания | S07,S09 | сводка: темы с истёкшим `revalidate_after_days`, материалы с `stale: true` или `delta`. Материал, чей `covers_version` не совпадает ни с одним значением `version_pins`, помечается «пин неизвестен» — сопоставить материал с конкретным пином нельзя, у материала нет ключа пина | T2 |
| S52 | Статистика попыток | S14,S36 | считается по тому, что в формате есть: доля `hinted`, распределение `per_question[].result` по типам вопросов, частота `next_action`, серии провалов. Поле `calibration` вердикта — свободная проза, в статистику не агрегируется, показывается как есть. На выборке меньше пяти попыток вывод не делается, а говорится, что данных мало | T2 |
| S53 | Шифрование конспектов | S34,S38,S39,S47 | два получателя age по notes.md (файл снят в S85, раздел «Шифрование»): ключ устройства в системном хранилище и парольная фраза; потеря обоих означает потерю конспектов, и это сказано до включения. Область — **только** внутренний каталог `<app-data>/notes`, шифрование и внешний каталог взаимоисключающи. Наблюдаемо: watcher каталога остановлен, кнопка «открыть в Obsidian» скрыта, файлы на диске нечитаемы редактором, имена файлов не выдают `topic_id`, индекс поиска (S39) зашифрован вместе с конспектами, экспорт (S47) предупреждает, что пишет открытый текст. Включение и выключение атомарны: прерывание не оставляет каталог наполовину зашифрованным | T2 |
| S54 | Плагин Obsidian | S34,S15,S53 | отдельный пакет в репозитории; показывает статус темы в заметке; читает те же файлы, ничего своего не пишет; при включённом шифровании (S53) не видит ничего и говорит об этом, а не показывает пустоту. Обработчик `tolearn://` валидирует входящую ссылку, отвергает неизвестный `roadmap`/`topic` и не выполняет ничего кроме навигации — проверяется вызовом обработчика напрямую; регистрация схемы в ОС — часть упаковки (S57), сквозной сценарий проверяется после неё | T3 |

## Слой 8 — голосовой зачёт

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S55 | Захват аудио и локальное распознавание | S38 | whisper.cpp, мультиязычная модель, без единого сетевого запроса. Здесь же заводится корпус аудиофикстур (в S02 его нет: тот про бандлы) и **назначаются замером** три числа, которых сегодня не существует, — WER, задержка на минуту речи и вес модели; каждое пиннится тестом и попадает в [бюджеты](../architecture.md#бюджеты), после чего ухудшение ломает сборку. Лицензия весов и атрибуция зафиксированы в `THIRD-PARTY.md`, отсутствие файла роняет CI. Запись не сохраняется на диск; манифесты микрофона объявлены здесь; разрешение ОС запрашивается по факту использования | T2 |
| S56 | Голосовой ответ | S55,S35 | расшифровка попадает в редактируемое поле ответа и правится руками до отправки; язык следует языку программы; при выключенной фиче точка входа **видна и неактивна** с пометкой «в разработке» и ссылкой на второй вариант, а не спрятана | T3 |

## Слой 9 — поставка

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S57 | Сборка и упаковка | S30–S40 | одна ответственность: превратить дерево в устанавливаемый артефакт. Сборки под macOS, Windows и Linux из CI; размер и вес JS уложены в [бюджеты](../architecture.md#бюджеты) и ломают сборку при превышении — гейт применяется к базовому варианту, у `-with-speech` порога до S55 не существует; установщик регистрирует URI-схему `tolearn://` и включает объявленные манифесты | T2 |
| S58 | Варианты дистрибутива и релизная страница | S56,S57 | флаг сборки живёт здесь: базовый и `-with-speech` собираются из одного дерева, различаются одним флагом, и оба варианта собираются в CI; на странице релиза сказано, чем они отличаются и сколько весят | T2 |

## Слой 10 — инструмент и долг

Три спеки не про новые возможности, а про то, что уже сделано и работает не так:
проверка стоит дороже, чем должна (S59), кнопка офлайна не отличает «не скачано»
от «скачано и свежо» (S60), сохранённая настройка не доживает до следующего
запуска (S61). Четвёртая, S68, — долг S60: та довела до ума кнопку темы и честно
оставила кнопку уровня программы за бортом. Пятая, S69, — долг S66: зачёт
разговором есть, но ответ модели показывается сырым текстом одной простынёй,
хотя приходит он размеченным.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S59 | Быстрый мутационный прогон | S01 | вердикт по одной мутации в `core` получается за минуты, а не за 15–20: замер «до» и «после» записан в [checks.md](../../.context/checks.md). `scripts/mutate.sh` печатает прогресс `N/M`, пишет лог построчно в файл, у каждого шага свой таймаут, у прогона — общий; шаг за таймаутом помечается `timeout` и не считается ни убитой мутацией, ни выжившей. Вердикт «убита» ставится по **близкому кругу** (тест-бинарники крейта мутации, `--fail-fast`), вердикт «выжила» — **только** после полного `--no-fail-fast` по всему воркспейсу; подставная мутация, которую близкий круг не ловит, а дальний ловит, доказывает эскалацию | T2 |
| S60 | Свежесть офлайн-материалов | S24,S32 | кнопка в шапке материалов темы имеет четыре наблюдаемых состояния вместо одного вечного «Сохранить»: есть нескачанное → «Сохранить»; всё скачано и окно 6 ч прошло → «Обновить скачанное»; всё скачано и окно не прошло → `disabled` с текстом «актуально на `DD-MM HH:MM`»; прошлый прогон оставил провалившиеся → «Повторить». Обновление трогает **только** изменившееся: `304` на условный запрос не пишет ни байта в хранилище, правка вне `<body>` перекачки не вызывает, правка внутри `<body>` — вызывает; отчёт перечисляет обновлённое поимённо. Индекс мигрирует на месте: база предыдущей версии открывается и работает | T2 + T3 |
| S61 | Настройки доживают до следующего запуска | S38 | язык, выбранный в настройках, остаётся выбранным после перезапуска приложения — проверяется запуском, а не рассуждением. Источник правды — `settings.yaml`, `localStorage` — зеркало: старт спрашивает бэкенд, чинит зеркало при расхождении и только потом решает, куда идти; молчащий бэкенд откатывается на зеркало, `navigator.language` остаётся последним резервом. Тема рисуется по зеркалу до первой отрисовки и чинится тем же ответом. Сохранение успевает до навигации. Тема лечится тем же швом и той же проверкой. Регрессионный тест пиннит оба дефекта: слушатель на ссылке острова и старт, не спрашивающий бэкенд | T4 |
| S68 | Выгрузка и обновление всей программы | S60 | на экране программы одна кнопка приводит офлайн **всей** программы к актуальному состоянию. До старта она называет цену: сколько всего материалов, сколько из них уже в хранилище, сколько занято из бюджета — и, если остатка не хватает, прямо говорит, что старое из **других** программ будет вытеснено, и ждёт подтверждения; влезает — запускается без вопросов. Режим выбирается **на каждый материал**, а не на прогон: нет в хранилище → скачать, свежее в окне 6 ч → не трогать вовсе (ни байта в сеть), устаревшее → условный запрос. Ход виден как «тема N из M · материал K из T · заголовок текущего», отмена срабатывает между материалами и уже сохранённое остаётся. Итог — сводка «скачано / пропущено как свежее / упало» и поимённый список **только** упавших с причинами; «Повторить» берёт ровно их. Материал, попавший в две темы, качается один раз | T2 + T3 |
| S69 | Разметка текста модели и код по клику | S66 | текст, пришедший от модели, показывается разметкой, а не простынёй: абзацы, заголовки, цитаты, списки — включая слипшийся в одну строку «1. … 2. …» и пункт, продолженный следующими строками (продолжение остаётся внутри пункта, нумерация сквозная), огороженные блоки кода дословно, inline-код, жирный, курсив и ссылки **только** на `http(s)`. Клик по любому коду — и по инлайновому, и по блоку — кладёт его текст в буфер и подтверждает тостом; буфер недоступен → тост говорит скопировать вручную, а не молчит. Разметка одна на весь текст модели: диалоговый зачёт, сквозной зачёт, подсказки. Бюджет JS экрана не пробит | T2 + T3 |

Почему у S59 два круга. Полный прогон после правки одного файла в `core` — это
5,5 минуты **только сборки** (замер на 12 ядрах, `cargo build --workspace
--tests` после `touch`), потому что от `core` зависят все крейты, включая `app`
с его 26 тест-бинарниками и линковкой Tauri. Обратная зависимость по графу
крейтов ничего не сокращает: закрытие `core` — это весь воркспейс. Сокращает
другое: чтобы **убить** мутацию, достаточно одного упавшего теста, и он почти
всегда лежит в крейте самой мутации. Поэтому дешёвый круг с `--fail-fast`
отвечает на «убита» за десятки секунд, а дорогой полный прогон платится только
за выжившие — то есть за находки, которых по определению мало. Строгость от
этого не падает: «выжила» по-прежнему означает «не упал ни один тест
воркспейса», просто это больше не цена каждой мутации.

Что в S60 считается доказательством свежести. Сначала время: `checked_at` в
индексе младше 6 часов — материал свеж, сеть не трогается вовсе. Дальше
условный запрос с `If-None-Match`/`If-Modified-Since` из уже хранимых `etag` и
`last_modified`: `304` — доказательство. Сервер без валидаторов отвечает `200`,
и тогда решает хэш: sha256 нормализованного `<body>` **сырого** ответа. Сырого,
потому что сравнивать можно только одинаковые представления, а запечатанная
`monolith`-страница включает встроенные ресурсы и меняется от прогона к
прогону; `<body>` без `<head>` — потому что счётчики, рекламные теги и версии
ассетов живут именно там и меняются постоянно, ничего не говоря о содержании.
Материалы `repo` и `video` из проверки исключены и помечаются «не
проверяется»: у клона и у видео нет ни `<body>`, ни дешёвого валидатора.

Чего в S60 нет. Кнопки уровня программы («выгрузить и обновить всё») — она
живёт на другом экране, у неё свой прогресс и своя отмена. Её спека — S68.

Что в S68 действительно новое. Не «взять все темы»: `save_offline` с пустым
`topic` уже собирает материалы всей программы и уже дедуплицирует их по URL
(`every` в `app/src/ipc/handlers/save_offline.rs`). Новое — три вещи, которых
на масштабе одной темы не видно.

Первая: **режим на материал вместо режима на прогон**. Сегодня режим один на
весь набор, и правило «есть хоть один нескачанный → весь набор идёт как
`Mode::Save`» на программе срабатывает почти всегда: одна новая тема означает
перекачку всего остального. Решение переносится внутрь очереди, к каждому
материалу: нет в хранилище — скачать, свежее в окне — не трогать вовсе,
устаревшее — условный запрос. Это правка `offline`, а не UI, и именно она
делает кнопку пригодной для повторного нажатия.

Вторая: **цена, названная до старта**. Программа — это сотни материалов и
зеркала с потолком 128 МБ каждое, поэтому прогон способен выбрать бюджет
целиком, а вытеснение по LRU снесёт офлайн соседней программы (защищена только
текущая). Точный вес заранее неизвестен — нескачанное не взвесить, не скачав;
называется поэтому то, что известно: сколько всего материалов, сколько уже в
хранилище, сколько занято из бюджета. Не хватает остатка — приложение прямо
говорит, чьи данные пострадают, и ждёт подтверждения; хватает — не спрашивает
ничего.

Третья: **отчёт, который читается**. Поимённый список на сотне материалов
никто не прочтёт, поэтому итог — сводка тремя числами плюс поимённо только
упавшие, а ход показывается двумя счётчиками сразу: тема из тем и материал из
материалов. Второе требует того, чего в работе сейчас нет: `Job` знает URL, но
не знает темы, — значит материалы едут в очередь вместе с темой-владельцем.

Два дефекта S61 найдены чтением и подтверждены на диске. Первый:
`RememberLocale.astro` вешает слушатели на `[data-locale]` в момент разбора
страницы, а ссылки языка в настройках рисует остров после гидратации — до них
слушатель не доживает, и зеркало не пишется никогда. Второй: `index.astro` при
старте бэкенд не спрашивает вообще, цепочка «зеркало → `navigator.language` →
`ru`» обрывается на языке системы. В `~/.config/tolearn/settings.yaml` при этом
лежит `locale: ru` — то есть запись работает, а чтение при старте отсутствует
как таковое.

## v2 — поэтапное обучение

Отзыв на первое MVP, решения интервью и черновая нарезка по группам —
в [.context/specs/2026-09-10-v2-analysis.md](../../.context/specs/2026-09-10-v2-analysis.md).
Здесь нарезаны первые три шага порядка — харнесс, решения, уход конспектов — и
группа B: формат v2, пакет `.tolearn` и уход v1 (слои 14–17). IA и холст дизайна
(группа F) — слой 18, генерация (группа C) — слои 19–20, обе по интервью
2026-09-12. Цикл обучения, иерархия, реализация интерфейса и диалоги обмена
нарезаются к интервью своих групп. Удаление старого — всегда отдельная спека, а не пункт
внутри новой: у каждого удаления свой наблюдаемый DoD, и новая спека не
закрывается чужой уборкой.

### Слой 11 — харнесс без чужого контекста

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S70 | Пресет `claude` без чужого контекста | S62 | аргументы пресета — `-p --output-format stream-json --verbose --include-partial-messages --tools "" --system-prompt <текст> --setting-sources project --strict-mcp-config --settings '{"env":{"CLAUDE_CODE_DISABLE_TERMINAL_TITLE":"1"}}'`; `--allowedTools ""` уходит: это список разрешений, он не отключает ни одного инструмента. Системный промпт нейтрален к задаче — одна фраза «выполни инструкцию из сообщения, ответь только результатом»: задача целиком едет в stdin, и один пресет годится генерации и проверке. Тест пиннит набор аргументов и то, что после вариадического `--tools` не стоит ни одного позиционного аргумента — промпт только в stdin. У каждого нового аргумента есть строка «зачем» в подсказках обеих локалей. `--settings` гасит побочный вызов CLI на haiku — генерацию заголовка терминала: env харнессу приложение не передаёт, поэтому переменная едет аргументом. Замер тем же набором аргументов, 5 прогонов, каждый из свежего пустого временного каталога: ≤ 1 000 входных токенов на вызов суммарно по всем моделям `modelUsage`, haiku ни в одном (до правки — около 27 тыс.), число, дата и версия `claude` записаны в [checks.md](../../.context/checks.md). `--bare` не берётся — он не видит логин из кейчейна | T2 + замер |
| S71 | Устаревшие аргументы сохранённого харнесса | S70 | сохранённый конфиг, чьи `args` не совпадают с советом пресета текущей версии, не переписывается молча: в настройках провайдера — плашка «аргументы пресета обновились» с разницей (что уйдёт, что добавится) и двумя действиями, «Обновить» и «Оставить мои». Отказ хранится в конфиге провайдера отпечатком совета и живёт до следующей смены совета, а не навсегда. Пустые `args` у пресета с советом — тот же случай; у `custom` плашки не бывает. Сравнение — чистая функция в `provider`, упорядоченное: перестановка тех же аргументов — тоже расхождение, и плашка так и говорит — «изменился порядок»; разница (что уйдёт, что добавится) считается мультимножеством, пустой аргумент рисуется `""`. Плашка видна, только пока форма совпадает с сохранённым конфигом: её кнопки сохраняют сами и иначе унесли бы несохранённые правки. Unit-тесты на совпадение, расхождение, перестановку, пустые `args`, `custom` и запомненный отказ | T2 + T3 |
| S72 | Чужой контекст у пресетов opencode и pi | S62 | по одному микро-вызову каждым пресетом из пустого временного каталога, входные токены записаны в [checks.md](../../.context/checks.md) рядом с замером S70. CLI везёт чужой контекст (глобальные правила агента, инструменты, сессии, MCP) и умеет его отключать — флаг в пресете, в тесте аргументов и в подсказке «зачем»; не умеет — подсказка под пресетом говорит это прямо. CLI не сообщает расход — это записано, и подсказка говорит «расход не проверен» | T2 + замер |
| S87 | opencode и pi временно недоступны | S70 | в выборе пресета харнесса opencode и pi — неактивные пункты с пометкой «позже» / «later», выбрать их нельзя. Сохранённый конфиг с одним из них открывается как есть: пункт выбран и помечен, вызовы не блокируются. Доступность — флаг пресета в `provider`, он же поле `available` в `PresetView`; вернуть пресет — сменить флаг, код пресетов и подсказки «зачем» остаются. `claude`, `custom` и HTTP-провайдеры не затронуты. Тесты: флаг у каждого пресета (`provider`), поле в ответе IPC (`app`), неактивный пункт с пометкой и открытие сохранённого конфига (`ui`) | T2 |

Почему харнесс — первая спека v2. Каждый вызов пресета `claude` вёз около 27
тыс. входных токенов чужого контекста: системный промпт Claude Code, описания
инструментов, глобальный CLAUDE.md, MCP. На программу с кругами починки это
1,3–2,7 млн токенов одних накладных, и чужие правила попадали в текст
генерации. Любой замер стоимости v2 до этой правки мерил бы накладные, а не
генерацию.

Почему S71 отдельно от S70. S70 меняет совет, S71 — судьбу уже сохранённых
конфигов: советы попадают в конфиг только кнопкой, и тот, кто её нажимал,
сидит на `--allowedTools ""`, пока ему не сказать. Проверяются они по-разному:
S70 — замером, S71 — экраном.

Почему S87. Пресеты opencode и pi не проверены: S72 упёрлась в креды чужих
CLI. В интерфейсе остаётся только проверенное, поэтому оба пресета закрыты до
конца v2, а S72 отложена туда же. Закрыты они только в выборе: код, подсказки
и уже сохранённые конфиги остаются, иначе возврат стоил бы отдельной спеки.

### Слой 12 — решения v2

Спека-ADR закрыта, когда: файл `docs/adr/0NN-*.md` описывает решение,
отвергнутые альтернативы и условие пересмотра; решение стоит строкой в таблице
[architecture.md](../architecture.md#решения); каждый документ, где действовало
отменённое правило, переписан в том же коммите — они перечислены в DoD;
уточняемый старый ADR получает одну строку-ссылку на новый, а текст его решения
не правится задним числом. Проверка — `app/tests/docs.rs` (ссылки, зеркальность
локалей) и `rg` по отменённой формулировке, который ничего не находит вне
истории (`progress.md`, `docs/*/release.md`).

ADR нумеруются с 012: номер 011 в истории занят удалённым решением об удалённом
доступе (`9acb7bd`), и переиспользовать его — развести две разные вещи под одним
именем.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S73 | ADR-012: модель нужна генерации, а не чтению | — | ограничения 1–2 [architecture.md](../architecture.md#ограничения-из-которых-всё-следует) переписаны: уже сгенерированное читается без сети, ключа и модели; генерация карты и этапов требует сети всегда, даже с локальной моделью, и проверяет её до вызова модели; «Уточнить» и проверка ответов требуют провайдера (локальная модель им считается), а сети — только с удалённым; ключ и аккаунт не обязательны; копипаст остаётся только для зачёта. Тесты по-прежнему без сети. В том же коммите: правило 3 `.context/README.md`, «Что нельзя» в `CLAUDE.md`, раздел «В работе (v2)» в `README.md` и `README.ru.md` — шапка README описывает v1, пока v2 не выпущена | T2 |
| S74 | ADR-013: программа растёт поэтапно, генерирует приложение | S73 | ограничение 3 переписано: программу генерирует приложение этап за этапом, и она растёт по мере прохождения. В ADR: грубая карта до первого этапа (цель, подпрограммы, часы — по строке); этап 2–4 ч — теория, практика, вопросы; программа 50–70 ч, то есть 15–25 этапов, больше — подпрограммы, не глубже трёх уровней; родитель — контейнер (цель, порядок детей, сводный прогресс), этапы только у листьев; после этапа — до трёх вариантов следующего, ровно один рекомендован. [ADR-007](../adr/007-incomplete-bundle.md) получает строку «уточнено ADR-013» | T2 |
| S75 | ADR-014: материал — текст модели, источники проверяет приложение | S74 | текст урока пишет модель по источникам, которые предлагает она же, а проверяет приложение: книга — Open Library по ISBN или названию, страница — загрузкой, изображение — Wikimedia Commons с лицензией и атрибуцией; модели уходит извлечённый текст. Учебник — ссылка уровня главы, цитат из книг нет. Класс источника следует `volatility`: `stable` — учебник, `evolving` — учебник и документация, `volatile` — документация и релизы. Схемы — Mermaid, отрисованные в SVG при генерации (S37); сломанная деградирует до исходника. Решено и записано, что происходит с генерацией без сети — проверке источников она нужна. Офлайн-выгрузка ссылок (S18–S24, S37, S60, S68) уходит на второй план и без отдельного решения не удаляется | T2 |
| S76 | ADR-015: слой уточнений вне бандла | S75 | «Уточнить» не пишет в бандл: уточнение — врезка отдельным блоком после абзаца, привязанная к `id` блока и хранимая в app-data; оригинал не трогается; врезку можно свернуть и убрать; перегенерация этапа её переживает, а врезка исчезнувшего блока становится осиротевшей по правилу [ADR-009](../adr/009-user-data-outside-bundle.md), а не удаляется; при экспорте её можно вшить. Правило «в бандл пишется только `progress.yaml`» остаётся в силе. ADR-009 получает строку «уточнено ADR-015» | T2 |
| S77 | ADR-016: формат v2 без совместимости с v1 | S74 | формат v1 уходит целиком вместе с тестами и эталоном `examples/llm-agents-base`; программа v1 не открывается, и приложение говорит это прямо, а не падает на разборе. Схемы v2 получают своё имя вместо `learning-roadmap/*` — внешнего генератора этого семейства больше нет (S86). Правило «эталон менять нельзя» в `.context/README.md` переписано: действует для v1, пока эталон v2 не заведён в группе B, дальше — для v2. Удаление кода v1 — отдельные спеки группы B | T2 |
| S78 | ADR-017: зачёт только письменный | S73 | зачёт — письменный, все ответы одним запросом, вердикт тем же протоколом `per_question` (S13, S14); зачёт необязателен, отказ от него не блокирует следующий этап; копипаст остаётся для зачёта. Диалоговый зачёт (S66) и зачёт по программе (S67) удаляются отдельными спеками группы D вместе с `exam-turn.md` и `exam-hint.md`; разметка S69 остаётся — ею рисуются урок и уточнения | T2 |
| S79 | ADR-018: отказ от конспектов | — | конспекты (S34), их шифрование (S53), плагин Obsidian (S54), конспекты в поиске (S39) и экспорте (S47) уходят; внешние программы для заметок рекомендуются в «Помощи» (группа F). Уже написанные файлы приложение не читает, не удаляет и не упоминает: ни выгрузки, ни инструкции нет — решение владельца от 2026-09-10; ADR прямо называет цену: зашифрованное открывается только внешним `age` по парольной фразе. Строка «конспекты» в таблице ADR-009 согласована с отказом. Удаление кода — S81–S85 | T2 |
| S80 | ADR-019: шрифт приложения — локальный | — | правило «никаких веб-шрифтов» уточнено: шрифт по сети запрещён, как и был; одно семейство в дистрибутиве разрешено — лицензия только OFL, запись в `THIRD-PARTY.md`, вес входит в потолок установщика, отдельный порог назначается замером при выборе шрифта в группе F. Код — системный моноширинный стек, как сегодня. Правило анимаций не меняется: только объясняющие, 120–200 мс, `prefers-reduced-motion`. В том же коммите: правило 1 `.context/README.md`, `.context/conventions.md`, раздел шрифтов [visual-system.md](../design/visual-system.md) | T2 |

### Слой 13 — конспекты уходят

Порядок — от листьев к ядру. Плагин читает состояние шифрования; индекс поиска
шифруется вместе с конспектами; экспорт при включённом шифровании
предупреждает об открытом тексте. Поэтому сначала уходят потребители, потом
шифрование, последним — ядро конспектов, на которое к тому моменту никто не
опирается. Каждая спека уносит свой код, тесты, ключи локализации, ручные
сценарии и документацию; `make dev` зелёный после каждой.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S81 | Плагин Obsidian уходит | S79 | каталога `obsidian/` нет; ключи `obsidian-lint` и `obsidian-test` ушли из `.context/checks.json` и [checks.md](../../.context/checks.md), `scripts/check.sh` не ставит плагину `node_modules`. Ссылка `tolearn://` уходит целиком — её единственным потребителем был плагин: разбор `core/src/link`, доставка `app/src/link.rs`, команда `follow`, `tauri-plugin-deep-link` и объявление схемы в `tauri.conf.json`; `app/tests/packaging.rs` пиннит обратное — схема не объявлена. `tauri-plugin-single-instance` остаётся: вторую копию приложения он не пускает и без ссылок, а второй запуск разворачивает и фокусирует уже открытое окно — раньше это делала доставка ссылки. Плагина нет в `docs/` (архитектура, установка из исходников), README и `THIRD-PARTY.md` | T2 |
| S82 | Конспекты уходят из поиска | S79 | поиск S39 ищет по темам и материалам: вида `note` нет ни в запросе, ни в выдаче, индекс их не собирает; остальные тесты поиска зелёные без правки ожиданий. Индекс — кэш: индекс v1 с видом `note`, как и любой нечитаемый, считается пустым и пересобирается, поиск на нём не падает | T2 |
| S83 | Конспекты уходят из экспорта | S79 | экспорт S47 в Markdown без раздела конспектов и без предупреждения про открытый текст; вывод по-прежнему валиден по CommonMark и проходит markdownlint | T2 |
| S84 | Шифрование конспектов уходит | S81,S82,S83 | `age` нет в дереве зависимостей (`cargo tree -i age` пуст) и в `THIRD-PARTY.md`; нет команды IPC `encryption`, острова `Encryption`, секции настроек и записи `NOTES` в кейчейне (`keys` в `app/src/ipc/context.rs`); ключ, уже лежащий в кейчейне, приложение не трогает, как и файлы; ручной сценарий `platform/01-notes-encryption.md` удалён | T2 |
| S85 | Конспекты уходят | S84 | нет `core/src/notes`, команд IPC конспектов, страницы `/notes/`, секции конспектов на экране темы, поля каталога конспектов в настройках и его наблюдателя; `docs/ru/notes.md` и `docs/en/notes.md` удалены парой, ссылки на них переписаны; каталоги `<app-data>/notes` и внешний приложение не читает и не удаляет | T2 + T3 |
| S86 | Скилл `learning-roadmap` удаляется | S77 | каталога `~/.claude/skills/learning-roadmap/` нет — это правка вне репозитория, в конфигурации владельца; в репозитории `rg "skills/learning-roadmap"` вне истории (`progress.md`, `docs/*/release.md`) пуст. Имена схем `learning-roadmap/*` остаются до удаления v1 в группе B | T1 |

### Слой 14 — решения формата и обмена

Спеки-ADR закрываются по правилам слоя 12. Решения — интервью группы B
(2026-09-11), отвергнутые варианты перечислены в DoD.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S88 | ADR-020: программа v2 — библиотека, каталог, блоки | S76,S77 | программа живёт в библиотеке app-data: содержимое — `<data>/programs/<uuid>/`, пользовательское (прогресс, уточнения, ответы зачёта) — `<data>/state/<uuid>/`. В каталог содержимого пишут только генерация и импорт — это правило заменяет «в бандл пишется только `progress.yaml`»; бандл v1 до своего удаления (S108) живёт по старому. Идентичность — UUID, выданный при создании и неизменный, плюс читаемый slug. Каталог: `program.yaml` (цель, уровень, карта, метаданные источников — url, ISBN, глава, лицензия, `checked_at`), YAML на этап со списком блоков `{id, kind, text}`, `assets/` файлами — SVG и картинки с лицензией и атрибуцией; формат на диске не зависит от формата ответа модели. Виды блоков: `paragraph`, `heading`, `code`, `callout`, `diagram` (Mermaid, отрисованный в SVG-ассет), `image` (ассет с атрибуцией); текст — подмножество разметки S69. Подпрограммы — `children/<uuid>/` в каталоге родителя со своими UUID, глубина ≤ 3, этапы только у листьев. `id` блока — короткий префикс sha256 текста, совпадение в этапе — суффикс по порядку: неизменённый блок переживает перегенерацию, изменённый (и правка опечатки) сиротит свои уточнения. Практика этапа — модель v1: задача, результат, `constraints` и `acceptance` с `claim`/`expect` прозой, `check` необязательна, текст задачи — блоками; ADR-006 не меняется. Среда (S63) в программе не хранится — генерация берёт её с машины; текст источников — кэш генерации в app-data, не часть программы. Схемы — семейство `tolearn/*`. Отвергнуты: папка на выбор пользователя, Markdown этапов с `id` в атрибутах, SQLite на программу, slug как идентичность, подпрограммы отдельными программами со ссылками, порядковый и случайный `id` блока, `progress.yaml` внутри программы, текст источников в программе. В том же коммите: «Что нельзя» в `CLAUDE.md`, представление программы v2 в [architecture.md](../architecture.md) рядом с разделом v1; ADR-009, ADR-015 и ADR-016 получают строку «уточнено ADR-020» | T2 |
| S89 | ADR-021: обмен — пакет `.tolearn` | S88 | `.tolearn` — zip с каталогом программы и `manifest.json`: версия формата `tolearn/package/1` и sha256 каждого файла. Подписи нет — доверие на отправителе, и ADR говорит это прямо. Распаковка — защитами S48. Всегда едет содержимое с `program.yaml`; прогресс и уточнения — галочками, по умолчанию выключенными, уточнения при этом вшиваются в текст отдельными блоками; текст источников не едет — получатель перепроверяет источники перед следующим этапом, этап генерируется под его среду, готовые остаются как есть. Совпадение UUID — «обновить» или «копия»: обновление заменяет содержимое целиком, пользовательское переживает по `id`, до замены диалог «что уйдёт», включая этапы, сгенерированные у себя и отсутствующие во входящем; копия получает новые UUID всего дерева и родословную не хранит. Подпрограмма выгружается отдельно как самостоятельная программа. SVG показывается только через `<img src>`; пакет, где SVG несёт `<script>`, `on*`-атрибут или внешний `href`, отвергается с причиной, санитайзера нет. Команды `check` из пакета — только по явному действию. Markdown-экспорт S47 остаётся экспортом «для чтения». До группы G совпадение UUID даёт только копию; «обновить», «что уйдёт» и галочки — группа G. Отвергнуты: SQLite-файл, один документ с base64-ассетами, EPUB, «только добавить недостающее», «всегда копия», прогресс и уточнения в пакете по умолчанию, среда и текст источников в пакете, санитайзер со встраиванием SVG в разметку, `<img>` без проверки на импорте, `forked_from`. Условие пересмотра — каталог программ: тогда git-репозиторий вторым путём. ADR-009 получает строку «уточнено ADR-021» | T2 |
| S90 | ADR-022: что из v1 не переживает v2 | S88 | в группе B без замены уходят: очередь повторений S44, план и прогноз S45, граф S46, история версий S49, таймер практики S50, дайджест устаревания S51, статистика S52, очередь и переключатель офлайна S24, свежесть S60, выгрузка программы S68, генерация «+» S43/S65 (её заменяет группа C); вернуть любую — своей спекой с решением. Поиск S39 переживает и пересобирается на текст этапов. Практика с раннером, копипаст-зачёт, вердикт, статусы и сводка прогресса переживают (ADR-013, ADR-017) и пересобираются на v2 в группе D; их экраны v1 уходят в B, и между B и D программу в сборке dev можно только читать — цена названа в ADR. Диалоговый и сквозной зачёт S66/S67 уходят в B, а не в D: их вход — темы v1, которых после B нет; ADR-017 получает строку «уточнено ADR-022». Крейт `offline` остаётся: хранилище S18, загрузка S19 и читалка S20 нужны группе C для проверки источников, S21–S23 ждут решения группы C. Это отдельное решение, которого требовал ADR-014, — он получает строку «уточнено ADR-022». Порядок: эталон v2 → экраны читают v2 → уход ядра v1, приложение ни на одном коммите не остаётся без программы. Данные v1 в app-data (реестр, распакованные архивы, история, индекс поиска, офлайн-хранилище) v2 не читает и не удаляет; где они лежат и что их можно удалить руками — строка в `docs/ru/guide.md` и `docs/en/guide.md`, в «Помощи» — группа F. Отвергнуты: удалить `offline` целиком, перенести офлайн на источники v2, удалить v1 до эталона v2, отложить экраны и уход v1 до реализации дизайна, удалить данные v1 при первом запуске, перенести их в `legacy-v1/` | T2 |

### Слой 15 — функции v1 уходят

Порядок — от листьев к ядру, как в слое 13: сначала уходят функции, которые
стоят на v1 и не переживают v2, ядро v1 — последним, в слое 17. Каждая спека
уносит свой код, тесты, ключи локализации, ручные сценарии и документацию;
файлы в app-data не читает и не удаляет; `make dev` зелёный после каждой.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S91 | Диалоговый и сквозной зачёт уходят | S90 | нет острова `Dialog` и страницы `/exam/dialog/`, `core/src/sweep`, острова `Sweep` и страницы `/sweep/`, их команд IPC и промптов без потребителя (`exam-turn.md`, `exam-hint.md`); экран темы ведёт в зачёт S35/S42, как до S66. Голосовой ответ S56 входил только через `Dialog` и теряет вход: крейт `speech` и команды `speech_*` остаются без потребителя до письменного зачёта группы D. Разметка S69 и `core/src/read` остаются — ими рисуются урок и уточнения (ADR-017) | T2 |
| S92 | Очередь повторений и статистика уходят | S91 | нет `core/src/queue`, `core/src/stats`, команд IPC `queue`, `repeat`, `stats`, островов `Queue`, `Stats`, страниц `/queue/`, `/stats/` и пунктов навигации к ним. `next_review_at` в `progress.yaml` v1 разбирается, как прежде, — формат v1 уходит целиком в S108 | T2 |
| S93 | План, прогноз и таймер уходят | S90 | нет `core/src/plan`, `core/src/practice` (сессия и таймер), команды IPC `plan`, острова `Plan` и компонента `Timer`; экран практики S33 работает без таймера — запуск `check` и вердикт человека не изменились. Запись «таймбокс истёк» больше не пишется, уже записанные приложение не трогает | T2 |
| S94 | Граф, дайджест устаревания и история версий уходят | S90 | нет `core/src/graph`, `core/src/stale`, `core/src/history`, команд IPC `graph`, `stale`, `history*`, островов `Graph`, `Stale`, страниц `/graph/`, `/stale/`; перегенерация и слияние S10 больше не сохраняют прошлую версию; каталог истории в app-data приложение не читает и не удаляет | T2 |
| S95 | Офлайн-очередь уходит | S90 | нет очереди и переключателя S24, свежести S60 и выгрузки программы S68: команд IPC `offline_*`, `save_offline`, `stop_offline`, `read_offline`, кнопок офлайна на экранах программы и темы, острова `Read` и страницы `/read/`. `offline` не импортирует типы программы v1 (`Material`, `Topic`); хранилище S18, загрузка S19, читалка S20, пререндер S37 и S21–S23 с их тестами зелёные без правки ожиданий; бюджет диска в настройках остаётся. Сохранённое в офлайн-хранилище приложение не удаляет | T2 |
| S96 | Генерация «+» уходит | S90 | нет кнопки «+» на `/programs/`, острова `Generate`, команд IPC `generate_*`, `core/src/generate`, промптов `generate-roadmap.md` и `generate-topic.md`; настройки провайдера (S41, S62–S64) и харнесс остаются — на них встанет генерация группы C. Черновики генерации в app-data приложение не читает и не удаляет | T2 |

### Слой 16 — формат v2 и обмен

Эталон v2 приходит в приложение тем же путём, что чужая программа, — пакетом
`.tolearn`. Состояние `state/<uuid>/` заводит группа D вместе с первым
писателем: в B пользовательское писать нечему.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S97 | `id` блока | S88 | чистая функция в `core`: `id` — короткий префикс sha256 текста блока; блоки этапа с одинаковым текстом получают суффиксы по порядку появления. Тесты: один текст — один `id` на всех платформах (вектор запинен), изменённый символ — другой `id`, повтор в этапе — разные `id`, перестановка несовпадающих блоков их `id` не меняет. Длина префикса такова, что на корпусе фикстур совпадений без суффикса нет, и записана в ADR-020 | T2 |
| S98 | Схемы v2 и эталон «chiptune, этап 1» | S97 | JSON Schema `tolearn/program/1` и `tolearn/stage/1` в `docs/schemas/` описывают все поля; справочник полей v2 — `docs/format.md`. Эталон `examples/chiptune/` написан руками: `program.yaml` с картой и источниками (учебник с ISBN и главой, страница), этап 1 с блоками всех видов, практикой без `check`, вопросами зачёта с эталонными ответами, SVG-схемой и картинкой с лицензией и атрибуцией в `assets/`; `id` блоков посчитаны функцией S97. Корпус `fixtures/v2/{valid,broken}` покрывает то, чего в эталоне нет: подпрограммы глубиной 3, практику с `check`, каждое нарушение схемы. Схема принимает эталон и валидный корпус и отвергает битый. Правило «эталон менять нельзя» в `.context/README.md` и `fixtures/README.md` переходит на `examples/chiptune/` (ADR-016) | T2 |
| S99 | Чтение программы v2 в `core` | S98 | типы и разбор `program.yaml` и этапов с местом ошибки (файл, строка); проверка дерева: глубина ≤ 3, этапы только у листьев, UUID уникальны в дереве, `id` каждого блока совпадает с S97, ассет, на который ссылается блок, существует, у картинки есть лицензия и атрибуция. Эталон читается со всеми полями; каждая битая фикстура падает со своей причиной, а не с первой попавшейся | T2 |
| S100 | Библиотека программ | S99 | `core` перечисляет программы в `<data>/programs/`, читает по UUID и устанавливает программу целиком атомарно — во временный каталог рядом, затем `rename`; прерванная установка оставляет прежнее состояние целым (тест роняет запись посередине). Нечитаемый каталог помечен в списке с причиной и не роняет список. Установка с занятым UUID отказывает явной ошибкой — копию решает импорт. Файлы v1 в app-data библиотека не читает и не меняет — тест кладёт их рядом. Удаления программы в B нет | T2 |
| S101 | Экспорт `.tolearn` | S99 | `core` упаковывает каталог программы в zip с `manifest.json` (`tolearn/package/1`, sha256 каждого файла); в пакете только содержимое, без `state/` и кэша источников. Пакет детерминирован: две упаковки одной программы дают один sha256 архива — порядок файлов и время в zip фиксированы. Программа, не прошедшая S99, не пакуется. Подкаталог `children/<uuid>/` пакуется как самостоятельная программа. CLI: `tolearn pack <каталог> <файл.tolearn>` | T2 |
| S102 | Импорт `.tolearn` | S100,S101 | `core` распаковывает пакет защитами S48 (`core/src/archive`), проверяет версию манифеста, sha256 каждого файла и отсутствие файлов вне манифеста, программу — по S99, SVG — на `<script>`, `on*`-атрибуты и внешние `href`, и устанавливает её в библиотеку S100. Каждый отказ называет причину и файл, на каждый в корпусе есть битый пакет. Совпадение UUID до группы G даёт копию: новые UUID всему дереву, результат импорта говорит, что добавлена копия. Круг «упаковать → импортировать» на эталоне даёт ту же программу с тем же UUID. CLI: `tolearn unpack <файл.tolearn> <каталог>` с теми же проверками | T2 |
| S103 | Экраны читают v2 | S102 | `/programs/` — список библиотеки (название, цель, часы карты, подпрограммы); импорт принимает `.tolearn` перетаскиванием и выбором файла, отказ показывает причину, копия — уведомлением; папка или архив v1 получают ответ «программы v1 не открываются» (ADR-016), а не ошибку разбора. Экран программы — цель, карта, этапы сгенерированные и ещё нет, подпрограммы. Экран этапа рисует все виды блоков: текст — разметкой S69, код — блоком кода, схема и картинка — только через `<img src>`, под картинкой — лицензия и атрибуция; практика и вопросы видны без запуска и зачёта — они вернутся в группе D; у блоков есть якоря по `id`. Клавиатура и фокус по S40, обе локали. Ручной сценарий: импортировать эталон и прочитать этап 1 — скриншоты светлой и тёмной темы. Экраны темы, практики, зачёта и разбора v1 после этой спеки недостижимы и уходят в S106 | T2 + T3 |
| S104 | Markdown-экспорт под v2 | S103 | `core/src/export` читает программу v2: каталог с `index.md` (оглавление по этапам и подпрограммам) и `.md` на этап, ассеты рядом, ссылки относительные и резолвятся; вывод валиден по CommonMark и проходит markdownlint; поля из модели видимости — как в S47. CLI `tolearn export <каталог> <папка>`, на экране программы — выгрузка в выбранную папку. Экспорт v1 уходит с этой заменой | T2 |
| S105 | Поиск по этапам v2 | S103 | индекс S39 собирается из блоков этапов всех программ библиотеки; попадание ведёт на экран этапа к блоку по якорю `id`; индекс инкрементальный и работает офлайн, как был. Индекс v1, как любой нечитаемый, считается пустым и пересобирается (правило S82) | T2 + T3 |

### Слой 17 — ядро v1 уходит

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S106 | Экраны изучения v1 уходят | S103,S91,S93,S95 | нет экранов темы S32, практики S33, копипаст-зачёта S35, встроенного экзаменатора S42 и разбора S36: страниц `/topic/`, `/practice/`, `/exam/`, `/review/`, островов `Topic`, `Practice`, `Exam`, `Review`, их команд IPC и ручных сценариев. Крейт `runner` и ADR-006 остаются — на них группа D пересобирает практику; `docs/ru/protocol.md` и `docs/en/protocol.md` остаются справочником протокола для D | T2 |
| S107 | Импорт, реестр и CLI v1 уходят | S106 | нет импорта бандла v1 из папки и архива, реестра S15, слияния S10 и команд CLI формата v1 (`validate`, `scan`, `progress`, `exam`, `merge`); распаковщик `core/src/archive` с защитами S48 остаётся — им пользуется `.tolearn`. Реестр и распакованные бандлы в app-data приложение не читает и не удаляет | T2 |
| S108 | Формат v1 уходит | S92,S94,S96,S104,S105,S107 | нет модулей `core` формата v1 и того, что на них стоит: `roadmap`, `topic`, `progress`, `bundle`, `scan`, `status`, `summary`, `verdict`, `protocol`, `exam`, `review`, `prompt`; нет схем `docs/schemas/{roadmap,topic,progress}.schema.json`, `docs/fields.md`, фикстур v1 (`page`, `reader` и `audio` остаются), эталона `examples/llm-agents-base` и правила о нём. Раздел v1 в [architecture.md](../architecture.md) удалён, `docs/ru|en/guide.md` и `offline.md` переписаны под v2 или удалены парой. `rg "learning-roadmap/|llm-agents-base"` вне истории (`progress.md`, `docs/*/release.md`) пуст | T2 |

### Слой 18 — IA и холст дизайна

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S109 | Информационная архитектура v2 | S103 | `docs/design/ia.md` заменяет `navigation.md` и одна на все концепции холста: экраны с адресами и откуда в них приходят (библиотека, новая программа, программа, этап, зачёт, развилка, поиск, настройки, помощь), шапка, слои поверх экрана («Уточнить» с кругом «понятно?», меню программы, удаление, экспорт), состояния (пусто, нет сети или провайдера, идёт генерация, ждёт генерации), клавиатура. Удаление программы — только корневой, из её меню, с подтверждением: `programs/<uuid>/` и `state/<uuid>/` уходят в корзину ОС, а не стираются. Открытый вопрос анализа v2 про удаление закрыт | документ |
| S110 | Холст дизайна: три концепции | S109,S80 | холст Claude Design опубликован: концепции «Книга», «Карта», «Студия» на общей IA, у каждой шесть экранов — библиотека, новая программа, программа, этап с «Уточнить», письменный зачёт, развилка; тема светлая и тёмная переключается у каждого экрана; у концепции записаны мотивация и главный компромисс; цвета и контраст — из `tokens.css`, шрифт у концепции один, OFL с кириллицей (ADR-019), код — системный моноширинный; текст — из эталона «chiptune», не заглушки. Выбор концепции — за владельцем; реализация нарезается в группу F после выбора и после E | T3 (владелец) |

### Слой 19 — генерация: основа и источники

Решения интервью группы C от 2026-09-12. Генерация живёт в новом крейте
`generate`: он собирает `core`, `offline` и `provider` и не знает про Tauri —
схемы в SVG и события шагов идут через трейты, окно WebView подставляет `app`.
Сеть проверки источников — в `offline`, модель — в `provider`. Тесты сети не
видят: ответы Open Library, Wikimedia Commons и модели — записанные фикстуры.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S111 | ADR-023: конвейер генерации | S110 | ADR записывает решения интервью группы C. Крейт `generate` — как выше. Этап строится шагами: модель предлагает источники, приложение их проверяет (ADR-014) → текст этапа одним вызовом → инварианты проверяет код, до трёх кругов починки, и модели уходит только сломанное → Mermaid → SVG → запись. У каждого шага свой замер времени и токенов, текущий шаг виден. Этап собирается во временном каталоге и попадает в программу одним `rename`: после отмены или сбоя в `programs/` нет ничего нового. Проверенные источники и извлечённый текст лежат в `<data>/cache/<uuid>/` — это не часть программы, в `.tolearn` они не едут, и повтор их не перепроверяет. Карта до «Начать» переделывается уточнением запроса, по кругу; этап перегенерируется по кнопке, блоки с неизменным текстом сохраняют `id` — вопрос ADR-013 о перегенерации закрыт. В группе C все шаги идут одной моделью провайдера, разделение по классам моделей — после замера S128. Из `offline` берутся хранилище S18, загрузка S19, читалка S20 и пререндер S37; зеркало S21, repo S22 и видео S23 уходят. ADR-004 помечен замещённым, у ADR-013 и ADR-014 — строка «Уточнено: ADR-023». Отвергнутые варианты интервью записаны с причинами: один вызов на этап и поблочно; атомарно без кэша и черновик с продолжением; карта только до старта и только новый запрос; неиспользуемое в `offline` не трогать и написать `offline` заново | документ |
| S112 | Зеркало, repo и видео уходят | S111 | нет модулей `offline::{mirror, repo, video}`, их тестов (`mirror.rs`, `repo.rs`, `video.rs`, `tools.rs`) и фикстур, которыми больше никто не пользуется; `gix` нет в workspace — `cargo tree -i gix` пуст; подсказок установки `yt-dlp` и `ffmpeg` нет ни в локалях, ни в `docs/`; `THIRD-PARTY.md` и описание крейта обновлены. Хранилище S18, загрузка S19, читалка S20 и пререндер S37 с их тестами зелёные без правки ожиданий | T2 |
| S113 | Крейт `generate` и проверка сети | S111 | крейт `generate` в workspace зависит от `core`, `offline` и `provider`, `cargo tree -p tolearn-generate` не содержит `tauri`. Перед любым вызовом модели на карту или этап он проверяет сеть запросом к Open Library и Wikimedia Commons с таймаутом; нет ответа — отказ `generate.offline` с хостом и причиной, и модель не вызывается: тест на заглушках сети и провайдера считает вызовы модели — ноль. Отказ сети и отказ провайдера различаются кодом ошибки | T2 |
| S114 | Книга: Open Library | S113 | `offline` ищет книгу по ISBN, без ISBN — по названию и автору, и разбирает ответ в название, авторов и ISBN. Ненайденная книга — «не найдено», а не ошибка; сбой сети — ошибка с причиной. HTTP идёт через трейт `Source` загрузки S19; тесты — на записанных ответах Open Library: по ISBN, по названию, не найдено, битый JSON. Ручной прогон с сетью — `cargo run -p tolearn-offline --example book`, команда и ожидаемый вывод в [checks.md](../../.context/checks.md) | T2 |
| S115 | Страница и кэш источников | S112,S113 | страница, которую предложила модель, загружается S19 с пререндером S37 и проходит читалку S20; ошибка загрузки или `extracted: false` — источник не прошёл, с причиной. Сырой HTML ложится в хранилище S18 по бюджету диска, итог проверки — url, заголовок, извлечённый текст, `checked_at`, найденные книги — в `<data>/cache/<uuid>/`, вне `programs/`. Повторная проверка того же источника той же программы берёт кэш: тест считает запросы `Source` — ноль. Модели уходит извлечённый текст, обрезанный до лимита символов на страницу; лимит — константа, обрезку пиннит тест | T2 |
| S116 | Картинка: Wikimedia Commons | S113 | `offline` ищет файл на Commons по запросу модели и читает лицензию и автора из метаданных файла. Лицензия вне списка свободных (CC0, PD, CC BY, CC BY-SA) — картинка не прошла. Прошедшая скачивается миниатюрой Commons шириной не больше 1200 px, файл тяжелее 400 КБ отвергается; картинка с лицензией и атрибуцией уходит в этап блоком `image` с ассетом. Не нашлась — ссылка без картинки, а не ошибка генерации (ADR-014). Тесты — на записанных ответах API Commons | T2 |
| S117 | Mermaid → SVG | S113,S37 | `generate` рисует схему через трейт; реализация в `app` — скрытое окно WebView, как у пререндера S37, с `mermaid.js`, вшитым в сборку: версия запинена, лицензия в `THIRD-PARTY.md`, в сеть окно не ходит. SVG проходит проверки импорта S102 — без `<script>`, `on*` и внешних `href` — и ложится ассетом блока `diagram`. Сломанная разметка или таймаут дают блок `code` с исходником Mermaid, а не пустое место или отказ генерации. Установщик остаётся в [бюджете](../architecture.md#бюджеты), прибавка записана. Тесты `generate` — на заглушке рисования; живое окно — `cargo run -p tolearn-app --example mermaid` на трёх схемах, одна из них битая | T2 + T3 |

### Слой 20 — генерация: карта, этап, развилка

Промпты лежат файлами рядом с кодом `generate`, размер каждого пиннится
тестом: промпт, незаметно выросший на килобайт, дорожает в каждом вызове.
Модель отвечает YAML, разбор возвращает место ошибки. Интерфейс до реализации
дизайна минимальный — ровно то, без чего генерацию не запустить и не
проверить руками.

| № | Спека | Зависит | DoD | Проверка |
|---|---|---|---|---|
| S118 | Карта: промпт, разбор, дробление | S113 | модель по запросу, уровню и локали отвечает картой: цель, `volatility`, этапы с часами. Код проверяет карту: этап 2–4 ч, лист не больше 70 ч и 25 этапов (ADR-013); больше 70 ч — строки подпрограмм с часами, у каждой не больше 70 ч, глубина не больше трёх. Нарушения уходят модели списком, до трёх кругов починки, дальше — отказ с перечнем нарушений. Подпрограмма разворачивается тем же шагом, с целью и часами своей строки у родителя. Тесты на фикстурах ответов: плоская карта, раздробленная, битая, которая чинится, и битая, которая не чинится | T2 |
| S119 | Новая программа: карта на подтверждение | S118 | команды IPC `plan_program` (запрос и уровень → проверка сети S113 → карта) и `revise_plan` (карта и уточнение → новая карта, по кругу; прежняя карта едет в промпт). До «Начать» в `programs/` не пишется ничего: тест смотрит каталог после плана и трёх переделок. Имена команд не совпадают с удалёнными в S96 — `app/tests/draft.rs` держит оба списка | T2 |
| S120 | Этап: источники и текст | S115,S116,S118 | шаг источников: модель по карте, месту этапа и `volatility` предлагает книги, страницы и картинки, приложение проверяет их S114–S116; непрошедшие выброшены, модель один раз просят о замене, не прошла и замена — этап строится на оставшихся (ADR-014). Шаг текста — один вызов: модель получает карту, место этапа, извлечённый текст страниц и книги с главами и отвечает этапом v2 — блоки, практика, вопросы с эталонами. `id` блоков считает `core` (S97), модель их не пишет. Книга в тексте — только ссылкой уровня главы; блок со ссылкой на непроверенный источник — нарушение. Размер обоих промптов пиннится тестом | T2 |
| S121 | Инварианты этапа и починка | S120 | код проверяет этап: `stage::check` и чтение S99; часы 2–4; есть теория и практика с заданием и приёмкой; у вопросов есть эталоны; длина текста и число новых терминов в пределах; в первом этапе программы ровно один инструмент. Термины и инструменты модель перечисляет служебным полем ответа, в этап оно не пишется; пороги — константы, записаны в ADR-023. Нарушения уходят модели только со сломанными частями — блоком по `id`, практикой, вопросами; целые части не пересылаются, и их `id` после починки те же (тест). До трёх кругов; не сошлось — отказ с перечнем нарушений, этап не пишется | T2 |
| S122 | Сборка и атомарная запись этапа | S117,S119,S121 | схемы этапа рисуются S117, картинки S116 ложатся в `assets/` с лицензией и атрибуцией, новые источники дописываются в `program.yaml`. Этап собирается во временном каталоге и попадает в программу одним `rename`. «Начать» (`start_program`) сначала генерирует этап 1 и только потом ставит программу в библиотеку установкой S100. Прерванная посередине запись (тест роняет её, как в S100), отмена и сбой на любом шаге оставляют `programs/<uuid>/` прежним, читаемым по S99 и без временных файлов. Отмена останавливает вызов модели: процесс харнесса завершён, висящих нет (тест на заглушке харнесса). Каждый шаг шлёт события начала и конца | T2 |
| S123 | Учёт времени и токенов | S122 | каждый вызов модели и каждая проверка источника пишут запись в журнал запросов: шаг, длительность, модель, входные и выходные токены; провайдер не сообщил токены — «нет данных», а не ноль. Итог этапа и программы — сумма по шагам из журнала. Тест на заглушке провайдера сверяет записи с шагами конвейера | T2 |
| S124 | Развилка | S122 | после этапа `generate` предлагает до трёх вариантов следующего, у каждого — строка о том, чем он продолжает путь по карте; при нескольких вариантах ровно один рекомендован, ответ без рекомендации или с двумя идёт в круг починки. Варианты лежат в кэше программы до выбора. Выбор (`take_next`) проходит проверку сети и конвейер S120–S122, и выбранный вариант становится строкой карты на месте следующей в той же записи. Вариант один — развилки нет, он предлагается сразу. С последнего этапа листа развилка отказывает с причиной (`next.end`): переход к следующей подпрограмме — S129. Отказ и отмена не стирают журнал запросов программы и не оставляют в нём записей этапа, который не лёг (ADR-024) | T2 |
| S125 | Перегенерация этапа | S122 | `regenerate_stage` прогоняет конвейер для существующего этапа: источники берутся из кэша и не перепроверяются, этап заменяется той же атомарной записью. Набор источников этапа сборка сохраняет в `cache/<лист>/gathered/<этап>`; перегенерация пропускает шаг «Источники», а без набора собирает источники заново. В промпте — прежний текст этапа и просьба объяснить иначе. Перегенерируется любой созданный этап, первый и в подпрограмме тоже; карта не меняется. Блоки с неизменным текстом сохраняют `id`: тот же ответ модели — те же `id`, изменён один абзац — меняется `id` только у него (тест). Отмена и сбой оставляют прежний этап. Что прогресс и уточнения переживают замену, проверяет группа D на своём `state/<uuid>/`. Отказ и отмена не стирают журнал запросов программы и не оставляют в нём записей этапа, который не лёг (ADR-024) | T2 |
| S126 | Генерация из CLI | S123 | `tolearn new "<запрос>" --level <уровень> --out <каталог>` строит карту и первый этап в каталог, а не в библиотеку, `tolearn next <каталог>` — следующий этап по карте. Шаги, время и токены каждого печатаются по ходу. Без WebView схемы остаются исходником Mermaid, и вывод это говорит. Результат проходит `tolearn pack` и импорт S102. Решения интервью 2026-09-13 ([ADR-025](../adr/025-cli-generation.md)): каталог — свой корень данных, занятый программой — отказ; `tolearn next <каталог>` показывает варианты развилки, `--choice N` строит выбранный; провайдер — из настроек приложения или `--provider <файл>`; строки шагов в stderr, итог в stdout, `--json` — объектом. Отказы: без сети, без уровня, неизвестный флаг, занятый каталог. Тесты — на HTTP-заглушке модели без сети: подставной харнесс не отвечает заданной последовательностью | T2 |
| S127 | Экран «Новая программа» и шаги | S119,S124,S125 | минимальный интерфейс по [IA](../design/ia.md#новая-программа) до реализации дизайна: в библиотеке — «Новая программа»; `/new/` — запрос, уровень, проверка сети и провайдера, карта на подтверждение, «Изменить запрос», «Начать»; во время генерации — текущий шаг (источники, текст, починка N из 3, схемы, запись) и «Отменить»; отказ — с причиной и ссылкой в настройки. На этапе — «Перегенерировать этап», на `/next/` — варианты развилки с отмеченным рекомендованным, выбор генерирует этап и открывает его. Обе локали, клавиатура и фокус по S40, JS в бюджете страницы. Ручной сценарий с живым провайдером — скриншоты светлой и тёмной темы. Решения интервью 2026-09-13: уровень — свободный текст; уход со страницы генерацию не останавливает, она доходит сама; на `/next/` ведёт «Пропустить зачёт» в конце этапа, пока зачёта нет | T2 + T3 |
| S128 | Замер на эталонных запросах | S126,S127 | ручной прогон «chiptune» и «локальные LLM»: карта и первые три этапа на двух классах моделей провайдера. Чек-лист по каждому этапу — один инструмент в первом этапе, теория вместе с практикой, источники проверены, книги ссылкой уровня главы, схемы отрисованы или деградировали до исходника, часы в пределах — заполнен в [checks.md](../../.context/checks.md) вместе с временем и токенами по шагам. Минимальный класс модели для урока назначен и записан в ADR-023, открытый вопрос анализа v2 закрыт. Оценка всей программы сверена с 0,3–0,8 млн токенов ADR-013; выход за неё — запись о пересмотре размера этапа | T3 (замер) |
| S129 | Переход к следующей подпрограмме | S124 | после последнего этапа листа развилка предлагает первый этап следующей подпрограммы по карте родителя: подпрограмма разворачивается `plan::expand` до листа, как при «Начать», и её первый этап ложится той же сборкой и подменой S124. До неё такой запрос отказывает с `next.end`. Критический путь не блокирует. Отказ и отмена не стирают журнал запросов программы и не оставляют в нём записей этапа, который не лёг (ADR-024) | T2 |

## Критический путь

```
S01 S02 S03 S04 S05 S06 S05a S07 S08 S09 S10 S11 S12 S13 S14 S15
S16 S17
S25 S26 S27 S28 S29
S30 S31 S32 S33 S34 S35 S36 S40
```

Здесь появляется первая версия, которой можно пользоваться: импорт программы,
чтение тем, практика с раннером, конспекты, зачёт через копипаст, ручное ведение
прогресса, переживание перегенерации.

Офлайн (S18–S24, S37, S68), встроенный LLM (S41–S42, S62–S67), поиск,
настройки, генерация, расширения (S44–S54), голос (S55–S56) и упаковка
(S57–S58) — сверху, критический путь не блокируют. Порядок внутри пути
совпадает с колонкой «зависит»: ни одна спека не начинается раньше того, на чём
стоит.

S28 (локализация) стоит **до** экранов намеренно. Введённая после — она означает
переписывание каждого экрана.

### Критический путь v2

```
S70 S71 S87
S73 S74 S75 S76 S77 S78
S79 S81 S82 S83 S84 S85
S88 S89 S90
S91 S92 S93 S94 S95 S96
S97 S98 S99 S100 S101 S102 S103 S104 S105
S106 S107 S108
S109 S110
S111 S112 S113 S114 S115 S116 S117
S118 S119 S120 S121 S122 S123 S124 S125 S126 S127 S128
```

S70 — первой: пока харнесс везёт чужой контекст, любой замер стоимости
генерации мерит накладные. Решения идут до кода формата и генерации, потому
что меняют ограничения, на которые код опирается. Конспекты и функции v1, не
переживающие v2, уходят до формата v2 — это сужает поверхность, которую
придётся переделывать. Ядро v1 уходит последним: до S103 приложению нечего
открыть, кроме программы v1. Между S103 и группой D программу в сборке dev
можно только читать (ADR-022). S72, S80 и S86 путь
не блокируют: S72 — замер чужих CLI, отложен до конца v2 вместе с пресетами
opencode и pi (S87), S80 нужен к холсту дизайна, S86 — уборка вне репозитория
после S77.

Группа C идёт от основания к экрану. ADR-023 и уборка `offline` — до кода,
который на `offline` встанет. Проверки источников и схемы делаются по
отдельности, каждая со своими фикстурами. Потом карта, этап и его атомарная
запись, и только поверх готового конвейера — учёт, развилка, перегенерация,
CLI и экран. Замер S128 последний: он мерит конвейер целиком, а его итог —
класс модели и размер этапа — может вернуться правкой в ADR-013 и ADR-023.
